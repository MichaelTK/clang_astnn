

#python samples.py
python classifier.py -i ../profiles-split_excludeVariants --given_split

##samples_test = fetch_samples(os.path.join(path_samples, 'validation'))


# add kernel='linear' for feature raking